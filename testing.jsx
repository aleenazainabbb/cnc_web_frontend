/*import React from 'react'

const testing = () => {

    const onSubmit = (data) => {
        setLoading(true)
        login(data)
        .then((response) => {
            if(response.success) {
            toast.success(response.message || "login done")
            navigate('/admin')
            }
            else{
            toast.error(response.message || "login failed")
            }
            })
            .catch((error) => {
                toast.error(error.message>.data?.message || "error")

                })
                .finally(() => {
                    setLoading(false)
                    })
        }

  return (
    <div>
      
    </div>
  )
}

export default testing
*/